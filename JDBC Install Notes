Install MySQL(installer ~350MB) and MySQL workbench

JDBC Connector download and add to project properties as a library

jdbc:mysql://localhost:3306/db_name



For ubuntu:
sudo mysql -u root

mysql> ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'password';

mysql> show databases;
